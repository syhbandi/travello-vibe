import SignUpClient from "./SignUpClient";

export const metadata = {
  title: "Sign Up | Travello",
  description: "Create a new Travello account to start booking your dream vacations and explore exclusive deals.",
};

export default function SignUpPage() {
  return <SignUpClient />;
}
