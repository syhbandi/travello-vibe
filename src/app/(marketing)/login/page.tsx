import LoginClient from "./LoginClient";

export const metadata = {
  title: "Login | Travello",
  description: "Sign in to your Travello account to manage your bookings and explore exclusive deals.",
};

export default function LoginPage() {
  return <LoginClient />;
}
