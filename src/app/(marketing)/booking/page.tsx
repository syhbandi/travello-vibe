import BookingClient from "./BookingClient";

export const metadata = {
  title: "Bookings | Travello",
  description: "Book your next dream vacation with Travello. Secure your spot in our exclusive packages and destinations.",
};

export default function BookingPage() {
  return <BookingClient />;
}
