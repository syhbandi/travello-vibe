# Issue: Update Navbar and Main Page Content

## Tasks

### 1. Update Navigation Bar
- Locate the Navbar component (likely in `src/components/`).
- Remove the **"Hotels"** and **"Flights"** links from the navigation menu.
- Add new links for **"Packages"** and **"Contact"** to the navigation menu. Ensure the styling matches the existing links.

### 2. Update Main Page Layout
- Locate the main page file (likely `src/app/page.tsx`).
- Locate the existing **"Destinations"** section.
- Create and add a new **"Packages"** section.
- Ensure the new "Packages" section is placed exactly *after* the "Destinations" section.
- Design the "Packages" section to match the overall aesthetics of the application (you can look at existing components like `Destinations.tsx` or `Services.tsx` for inspiration).
